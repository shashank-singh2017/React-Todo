import './Header.css';

// A react component is a javascript function which returns Html content

function Header() {
    return (
        <div>
            <h1 id="textBox">My Todo App</h1>
        </div>
    );
}

export default Header;